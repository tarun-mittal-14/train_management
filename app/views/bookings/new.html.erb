<h1>New Booking</h1>

<%= form_with(model: [@train, @booking], url: train_bookings_path(@train)) do |form| %>
  <div class="field">
    <%= form.label :seats %>
    <%= form.number_field :seats, min: 1 %>
  </div>

  <%= form.submit 'Create Booking' %>
<% end %>

<%= link_to 'Back', root_path %>
